namespace Advent.Year2022.Day20;

public sealed class Answer : IPuzzle
{
    public int Part1(IEnumerable<string> input)
    {
        var moves = input.Select(int.Parse).ToArray();

        return 0;
    }

    public int Part2(IEnumerable<string> input)
    {
        return 0;
    }
}
